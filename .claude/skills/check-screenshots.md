Read the latest screenshots from the user's desktop for visual review.

Steps:
1. Run: `ls -t ~/Desktop/*.png | head -4` to find the most recent files
2. Read the latest 1-2 (or up to 4 if the user has 2 screens)
3. Describe what you see — layout, colours, any obvious issues
4. Flag anything that looks like a bug or design problem
